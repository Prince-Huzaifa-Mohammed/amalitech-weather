import styled from "styled-components";

export const Box = styled.div`
  padding-top: 2rem;

  h6 {
    margin-top: 5rem;
    text-align: center;
    font-size: 2rem;
    font-weight: 300;
  }

  nav {
    position: relative;
  }

  aside {
    position: absolute;
    right: 3rem;
    z-index: 5;
  }
`;
