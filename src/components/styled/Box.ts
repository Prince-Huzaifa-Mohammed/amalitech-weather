import styled from "styled-components";

export const Box = styled.div`
  padding-top: 2rem;

  nav {
    position: relative;
  }

  aside {
    position: absolute;
    right: 3rem;
    z-index: 5;
  }
`;
